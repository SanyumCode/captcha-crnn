# Contributing

感谢贡献。提交前请：

1. 从 Issue 开始说明问题或提案；大改动先讨论设计。
2. 创建小而聚焦的分支，不提交数据集、模型、IDE 配置或生成结果。
3. 使用 Python 3.11，安装 `requirements.txt`。
4. 运行 `python -m compileall -q src examples legacy tests` 和 `pytest -q`。
5. PR 中说明动机、行为变化、测试结果；涉及指标时附评估集和复现命令。

贡献即表示你同意按本仓库 MIT License 授权你的提交。
